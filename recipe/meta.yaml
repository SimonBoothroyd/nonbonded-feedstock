{% set name = "nonbonded" %}
{% set version = "0.0.1a2" %}
{% set sha256 = "39506c5b1c9555a8f79b6e6dd7174ae840ff366661299b1019c46f261064efbf" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/simonboothroyd/{{ name }}/archive/{{ version | replace("a", "-alpha.") }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv
  entry_points:
    - nonbonded = nonbonded.cli:cli

requirements:
  host:
    - python
    - pip
  run:

    - python

      # Core dependencies
    - click
    - click-option-group
    - numpy
    - pydantic
    - requests
    - pandas
    - jinja2
    - email_validator

      # Benchmark / optimization target dependencies
    - forcebalance >=1.7.5
    - openforcefield
    - openff-evaluator >=0.2.0
    - openff-recharge >=0.0.1a2

      # SMILES to SVG
    - rdkit

      # Plotting
    - seaborn >=0.11

      # Statistics
    - scipy

      # REST dependencies
    - sqlalchemy
    - sqlalchemy-utils

test:
  imports:
    - nonbonded

about:
  home: https://github.com/simonboothroyd/nonbonded
  license: MIT
  license_file: LICENSE
  description: Automated optimization and assesment of nonbonded force field parameters.

extra:
  recipe-maintainers:
    - simonboothroyd